defaults:
  - GradDiff

handler: CodeEraser
args:
  learning_rate: 3e-6
  lr_scheduler_type: constant
  weight_decay: 0.0
  per_device_train_batch_size: 1
  gradient_accumulation_steps: 32
method_args:
  select_gamma: 0.5
  control_alpha: 1.0
  control_lambda: 0.1
